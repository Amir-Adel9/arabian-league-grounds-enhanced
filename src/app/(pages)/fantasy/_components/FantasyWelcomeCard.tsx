import { motion } from 'framer-motion';
const FantasyWelcomeCard = ({}: {}) => {
  return (
    <motion.div className='flex flex-col justify-center'>
      FantasyWelcomeCard
    </motion.div>
  );
};

export default FantasyWelcomeCard;
